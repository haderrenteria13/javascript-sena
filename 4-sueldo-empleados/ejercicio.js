// desarrollar un algoritmo que asigne el sueldo a cinco empleados, teniendo en cuenta su categoría.

let categoria = parseInt(prompt("Ingrese la categoría del trabajador: "));

if (categoria === 1) {
    let sueldo1 = 200
    console.log(`El sueldo es de $${sueldo1}`);
}

if (categoria === 2) {
    let sueldo2 = 300
    console.log(`El sueldo es de $${sueldo2}`);
}

if (categoria === 3) {
    let sueldo3 = 400
    console.log(`El sueldo es de $${sueldo3}`);
}

if (categoria === 4) {
    let sueldo4 = 500
    console.log(`El sueldo es de $${sueldo4}`);
}

if (categoria === 5) {
    let sueldo5 = 600
    console.log(`El sueldo es de $${sueldo5}`);
}

