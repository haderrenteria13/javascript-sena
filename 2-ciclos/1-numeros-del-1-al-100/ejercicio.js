let Pregunta = prompt ("¿El numero es menor o mayor que 0?")
let num = Number(prompt("Digite cuantas veces quiere que cuente: "))
let contador = 0

if (Pregunta === "mayor") {
    while (num > contador) {
        contador++
        console.log(contador)
      } 
} else if (Pregunta === "menor"){
    while (num > contador) {
        contador++
        console.log(contador)
      } 
}

